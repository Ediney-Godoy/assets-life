# Asset Life root .env (development)
# Database configuration (PostgreSQL)
USE_SQLITE=0
DB_HOST=localhost
DB_PORT=5432
DB_NAME=assetlife
DB_USER=postgres
DB_PASSWORD=postgres

# Optional: single URL overrides DB_* vars
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/assetlife